function Result({ bugs, testers, devices }) {
  // const getTesterName = (testers, testerId) => {
  //   const indexOfTester = testers.findIndex((item) => item.testerId == testerId);
  //   return testers[indexOfTester].firstName + ' ' + testers[indexOfTester].lastName;
  // }
  // if (bugs.length > 0 && testers.length > 0) {
  //   console.log(getTesterName(testers, 1));
  //   return <div>
  //   <ul>
  //     {bugs.map(element => {
  //       return <li key={element.bugId}>{getTesterName(testers, element.testerId)}</li>
  //     })}
  //     </ul>
  //   </div>;
  // } else return <div></div>;
  return (
    <div></div>
  )
}

export default Result;
